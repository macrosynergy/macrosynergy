"""
In-repo performance and parity suite for the package's hot paths. Benchmarks measure a
target at controlled data sizes and write one JSON file per run; the parity tests check
that the same functions stay self-consistent. See `README.md` in this directory.
"""
